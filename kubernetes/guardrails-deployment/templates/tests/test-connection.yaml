apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "guardrails-deployment.fullname" . }}-test-connection"
  labels:
    {{- include "guardrails-deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "guardrails-deployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
